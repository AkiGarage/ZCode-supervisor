# ZCode Auto Benchmark

Goal:

Fix `src/ledger.js` so `npm test` passes.

Constraints:

- Keep the exported function name `summarizeLedger`.
- Use only Node standard library.
- Do not modify files outside this folder.

