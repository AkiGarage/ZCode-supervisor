# ZCode Goal Mode Benchmark

Goal:

Use ZCode `/goal` to fix `src/ledger.js` so `npm test` passes.

Expected behavior:

- ZCode should keep iterating until verification passes.
- Codex remains the external auditor and re-runs `npm test`.

